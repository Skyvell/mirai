# Copy to `.env.local` (untracked) and fill in.

# Clerk Publishable Key — dashboard.clerk.com -> API keys. Public, not a secret.
VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxx

# Backend API base URL.
VITE_API_URL=http://localhost:8000
