name: Deploy app

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string

concurrency:
  group: app-${{ inputs.environment }}
  cancel-in-progress: false

permissions:
  id-token: write
  contents: read

jobs:
  release:
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    env:
      IMAGE: ${{ vars.GCP_REGION }}-docker.pkg.dev/${{ vars.GCP_PROJECT_ID }}/mirai/mirai-api:${{ github.sha }}
      MIGRATE_JOB: mirai-migrate
    steps:
      - uses: actions/checkout@v6

      - id: auth
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ vars.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ vars.GCP_SERVICE_ACCOUNT }}
          token_format: access_token

      - uses: docker/login-action@v3
        with:
          registry: ${{ vars.GCP_REGION }}-docker.pkg.dev
          username: oauth2accesstoken
          password: ${{ steps.auth.outputs.access_token }}

      - name: Build and push
        run: |
          docker build -t "$IMAGE" backend
          docker push "$IMAGE"

      - uses: google-github-actions/setup-gcloud@v3

      # Migrations run before the service sees the new image; a failure here
      # aborts the release.
      - name: Run database migrations
        run: >
          gcloud run jobs update "$MIGRATE_JOB" --region "${{ vars.GCP_REGION }}"
          --image "$IMAGE" --execute-now --wait --quiet

      - name: Deploy to Cloud Run
        uses: google-github-actions/deploy-cloudrun@v3
        with:
          service: mirai-api
          region: ${{ vars.GCP_REGION }}
          image: ${{ env.IMAGE }}
